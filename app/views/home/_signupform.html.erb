<div class="form-inline text-center">
        <p><%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <div class="form-group">
            <%= f.email_field :email, :autofocus => true, :placeholder => "email", class: "form-control input-lg" %>
          </div>

          <div class="form-group">
            <%= f.password_field :password, :placeholder => "mot de passe", class: "form-control input-lg" %>
          </div>

          <div class="form-group">
            <%= f.password_field :password_confirmation, :placeholder => "confirmer mot de passe", class: "form-control input-lg" %>
          </div>

          <div class="form-group">
            <%= f.submit "s'inscrire sur Hackuna", class: "btn btn-primary btn-lg" %>
          </div>
          <!-- <br><span class="center"><small><%= t("home.you_accept_terms") %><%= link_to t("home.our_terms"), "#" %>)</small></span> -->
        <% end %>
      </p>
      </div>